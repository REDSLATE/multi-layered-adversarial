"""Camino native runtime — doctrine code (trend, opportunity hunter).

Mirrors `shared/brains/barracuda/`. Trend-following: BUY clear uptrends
above SMA(20)>SMA(50), SHORT clear downtrends (env-gated).
"""
